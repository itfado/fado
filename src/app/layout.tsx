// Root layout — minimal wrapper required by Next.js App Router.
// All fonts, metadata and locale setup live in app/[locale]/layout.tsx.
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return children
}
